spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
  profiles:
    active: test-container
  cloud:
    discovery:
      enabled: false
  security:
    enabled: false
